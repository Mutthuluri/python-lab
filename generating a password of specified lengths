import random
import string
def gp(lth):
  char=string.ascii_letters + string.digits
  password="".join(random.choice(char) for e in range(lth))
  return password
n = int(input())
ans = gp(n)
print(ans)
